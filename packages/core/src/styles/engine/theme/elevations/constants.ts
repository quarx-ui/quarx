import { QX_SIZE, valuesAsKeysFromArray } from '@core';

export const ELEVATION_SIZE = QX_SIZE;
export const ELEVATION_TYPE = valuesAsKeysFromArray([
    'main',
    'secondary',
]);
export const ELEVATION_COORDS = valuesAsKeysFromArray([
    'x',
    'y',
    'b',
    's',
]);
