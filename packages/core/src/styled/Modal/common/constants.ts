import { valuesAsKeysFromArray } from '@core';

export const MODAL_DIRECTION = valuesAsKeysFromArray([
    'vertical',
    'horizontal',
]);

export const MODAL_SCROLL_BEHAVIOR = valuesAsKeysFromArray([
    'body',
    'window',
]);
