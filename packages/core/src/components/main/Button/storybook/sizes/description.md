Параметр `size` изменяет высоту компонента и размер текста в нем. 
Существует четыре значения для передачи:

- `xSmall` — минимальный размер,
- `small` — маленький,
- `medium` — средний,
- `large` — наибольший размер.
