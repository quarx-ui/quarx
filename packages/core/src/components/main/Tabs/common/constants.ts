import { valuesAsKeysFromArray } from '@core';

export const TABS_TYPES = valuesAsKeysFromArray([
    'default',
    'contained',
    'segmented',
]);

export const TABS_SCROLL_POSITIONS = valuesAsKeysFromArray([
    'none',
    'start',
    'end',
    'between',
]);

export const TABS_FADE_WIDTH = 60;
