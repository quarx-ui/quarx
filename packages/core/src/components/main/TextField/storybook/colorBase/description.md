Параметр `colorBase` используется для выбора цвета текстового поля для разного фона.
Представлено 2 значения:
<br/>
`main` — для использования на белом фоне.
<br/>
`secondary` — используется на сером или другом фоне.