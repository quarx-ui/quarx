export interface CollapseStyleParams {
    /** Признак горизонтального направления скрытия элемента
     *
     * Передаётся в стили автоматически по свойству orientation
     *
     * @default horizontal */
    isHorizontal: boolean;
}
