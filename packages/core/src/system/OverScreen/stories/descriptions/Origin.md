Параметр `origins` определяет с какой стороны будет выезжать компонент при появлении. Работает только при `appearance` = `slide`.

Доступно 4 варианта:
`top`,
`bottom`,
`left`,
`right`.
<br>
По умолчанию используется свойство `right`.
