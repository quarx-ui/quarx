import { SnackbarSize } from '@core/src/main/Snackbar/elements/types';

export interface BodyStyleParams {
    /** Размер компонента
     * - small
     * - large
     * @default small */
    size: SnackbarSize;
}
