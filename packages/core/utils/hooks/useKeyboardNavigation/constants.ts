import { valuesAsKeysFromArray } from '../../object';

export const ARROW_KEYS = valuesAsKeysFromArray([
    'ArrowUp',
    'ArrowDown',
    'ArrowLeft',
    'ArrowRight',
]);

export const ORIENTATIONS = valuesAsKeysFromArray([
    'horizontal',
    'vertical',
]);
