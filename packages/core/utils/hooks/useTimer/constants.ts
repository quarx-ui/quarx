import { valuesAsKeysFromArray } from '@core/enums';

export const TIME_SCALE = 1000;
export const STEP = 1;

export const START_TIME = 5;
export const END_TIME = 0;

export const RERENDER_VALUES = valuesAsKeysFromArray([
    'step',
    'end',
]);
