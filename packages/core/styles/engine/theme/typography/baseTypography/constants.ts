import { valuesAsKeysFromArray } from '@core';

export const DEFAULT_FONT_SIZE = 14;
export const DEFAULT_BROWSER_FONT_SIZE = 16;

export const DEFAULT_HEADLINE_FONT_FAMILY = 'SB Sans Display, sans-serif';
export const DEFAULT_TEXT_FONT_FAMILY = 'SB Sans Text, sans-serif';

export const BASE_TYPOGRAPHY_TYPE = valuesAsKeysFromArray([
    'text',
    'headline',
]);
